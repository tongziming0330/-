 HEAD
# -
我的个人简历网站
# 佟子铭个人简历

这是一个纯静态网页项目，入口文件为 `index.html`。

## 自动部署

将本项目推送到 GitHub 后，在 Vercel 或 Netlify 中导入该仓库，并开启 Git 自动部署。以后每次推送到默认分支，平台都会自动重新部署，同时沿用原来的网址。

本项目不需要安装依赖，也不需要构建命令：

- Build command：留空
- Output directory：`.`（项目根目录）
- Install command：留空

## 本地预览

直接用浏览器打开 `index.html` 即可预览；也可以使用任意静态文件服务器运行项目 
10de794 (上传个人简历网站)
